<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CollegeSpace Dashboard</title>
    <!-- Add Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <!-- Add custom styles -->
    <style>
        body {
            background: linear-gradient(to bottom, #05386B, #05CDB9);
            color: #EDF5E1;
        }

        .header-section {
            background-color: #05386B;
            color: #EDF5E1;
            padding: 40px 0;
        }

        .feature-section {
            background-color: #EDF5E1;
            padding: 60px 0;
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .btn-primary {
            background-color: #05386B;
            border-color: #05386B;
        }

        .btn-primary:hover {
            background-color: #ffffff;
            color: #05386B;
        }
    </style>
</head>

<body>
    <div class="header-section text-center">
        <h1 class="display-4 fw-bold">COLLEGESPACE</h1>
        <p class="lead">Your Gateway to Educational Resources</p>
    </div>

    <div class="feature-section container py-5">
        <h2 class="pb-2 border-bottom " style="color: black; text-align: center;">Departments</h2>
        <div class="row g-4 py-5 row-cols-1 row-cols-lg-3">
            <% for (const department of departments) { %>
                <div class="col d-flex align-items-start">
                    <div
                        class="icon-square text-body-emphasis bg-body-secondary d-inline-flex align-items-center justify-content-center fs-4 flex-shrink-0 me-3">
                        <svg class="bi" width="1em" height="1em">
                            <use xlink:href="#toggles2"></use>
                        </svg>
                    </div>
                    <div>
                        <h3 class="fs-2 text-body-emphasis" style="color: black;">
                            <%= department.name %>
                        </h3>
                        <p style="color: black;">
                            <%= department.description %>
                        </p>
                        <a href="/department/<%= department._id %>" class="btn btn-primary">
                            Explore
                        </a>
                    </div>
                </div>
            <% } %>
        </div>
    </div>

    <footer class="text-center py-3">
        <p>&copy; 2024 CollegeSpace. All rights reserved.</p>
    </footer>

    <!-- Add Bootstrap JS (optional) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>

    <!-- Add script for logging departments -->
    <!-- <script>
        console.log('Departments:', <%= JSON.stringify(departments) %>);
    </script> -->
</body>

</html>
